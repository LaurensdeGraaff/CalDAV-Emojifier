# LLM Testing

Testing/experimenting with LLM.

## Setup
1. Create training data from your calendar or use `training_example.ics`
2. Ensure `training.ics` exists in the `prediction` folder
3. add your info to the emoji dict
4. start training

## Usage
```bash
cd prediction
python3 training.py
```